# 🚇 MosMetro Guide

Путеводитель по Москве через станции метро.  
Проект для изучения Python, SQL и работы с данными.

## 📊 Данные

- **250 станций** московского метро
- **970 достопримечательностей** в шаговой доступности
- **15+ линий** метро (включая МЦК, БКЛ, МЦД)

## 🗂️ Структура проекта

```
moscow-metro-guide/
├── stations.csv          # Исходные данные (станции, линии, места)
├── stations.json         # Данные в JSON для веб-сайта
├── init_db.py            # Загрузка данных в PostgreSQL
├── save_to_json.py       # Конвертация CSV → JSON
└── load_data.py          # Обработка CSV (группировка по станциям)
```

## 🗄️ База данных

Проект использует **PostgreSQL** с тремя таблицами:

| Таблица | Назначение |
|---------|------------|
| `lines` | Линии метро |
| `stations` | Станции метро (связь с lines) |
| `places` | Достопримечательности (связь со stations) |

## 🚀 Как запустить локально

### 1. Установите PostgreSQL
Скачайте с [postgresql.org](https://www.postgresql.org/download/)

### 2. Создайте базу данных
```sql
CREATE DATABASE moscow_metro;
```

### 3. Загрузите данные
```bash
python init_db.py
```

### 4. Проверьте через psql
```bash
psql -U postgres -d moscow_metro
SELECT COUNT(*) FROM places;  -- должно быть 970
```

## 📈 Примеры SQL-запросов

```sql
-- Топ-10 станций по количеству мест
SELECT s.name, COUNT(p.id) as places_count
FROM stations s
JOIN places p ON s.id = p.station_id
GROUP BY s.id
ORDER BY places_count DESC
LIMIT 10;

-- Все места у станции "Краснопресненская"
SELECT p.name, p.place_type, p.walking_minutes
FROM places p
JOIN stations s ON p.station_id = s.id
WHERE s.name = 'Краснопресненская';
```

## 📊 Результаты

Самая насыщенная станция — **Краснопресненская** (7 мест):

| Место | Тип |
|-------|-----|
| Московский зоопарк | Зоопарк |
| Планетарий | Музей/Культура |
| Башня 2000 (Москва-Сити) | Панорама |
| Красная Пресня (музей революции) | Музей |
| Улица Большая Грузинская | Улица |
| Аквариум «Москвариум» | Океанариум |
| Кафе «АндерСон» | Кафе |

## 🔜 Планы

- [ ] Telegram-бот для поиска мест
- [ ] Веб-сайт на Flask
- [ ] Форма добавления мест пользователями
- [ ] Docker-контейнер для лёгкого развёртывания

## 👤 Автор

**Светлана Борисенкова**  
GitHub: [@Sozercaya](https://github.com/Sozercaya)

---

⭐ Если проект полезен — поставьте звезду на GitHub!